mutation createNote($id: String!, $title: String, $content: String, $timestamp: Float) {
  createNote(id: $id, title: $title, content: $content, timestamp: $timestamp) {
      id
      title
      content
      timestamp
  }
}
