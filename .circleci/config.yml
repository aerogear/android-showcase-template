version: 2
jobs:
  build:
    working_directory: ~/code
    docker:
      - image: circleci/android:api-27-alpha
    environment:
      JVM_OPTS: -Xmx3200m
    steps:
      - checkout
      - restore_cache:
          key: jars-{{ checksum "build.gradle" }}
      - run:
          name: Print env var
          command: |
            echo $KEYSTORE_PASSWORD_BASE64 | base64 --decode > /tmp/keystorepass.txt
            echo $KEY_PASSWORD_BASE64 | base64 --decode > /tmp/keypass.txt
            echo $KRYPTOWIRE_API_KEY_BASE64 | base64 --decode > /tmp/apikey.txt
      - run:
          name: Chmod permissions
          command: sudo chmod +x ./gradlew
      - save_cache:
          paths:
            - ~/.gradle
          key: jars-{{ checksum "build.gradle" }}
      - run:
          name: Run Tests
          command: ./gradlew compileReleaseSources
      - run:
          name: Build release artifact
          command: |
            export KEYSTORE_PASSWORD=`cat /tmp/keystorepass.txt`
            export KEY_PASSWORD=`cat /tmp/keypass.txt`
            ./gradlew aR
      - run:
          name: Push to Krypowire for scanning
          command: |
            export  KRYPTOWIRE_API_KEY=`cat /tmp/apikey.txt`
            curl -X POST "https://emm.kryptowire.com/api/submit" -F app=@./app/build/outputs/apk/app-release.apk -F key=$KRYPTOWIRE_API_KEY -F platform=android
